---
const year = new Date().getFullYear();
---

<footer>
    <div class="footlinx">
        <span class="separator">𓂃⋆ * 𓇢𓇬</span>
        <a href="/">cassandraopeia</a>
        <span class="separator">𓋼𓍊 ❧ 𓍢ִ໋ .☘︎ ݁ </span>
        <a href="/contact">contact</a>
        <span class="separator"> 𓏲 ◌°☽ ⋆. </span>
        <a href="https://instagram.com/casi.ta" target="_blank">instagram</a>
        <span class="separator">𓇬𓇢 * ⋆𓂃</span>
    </div>

    <p class="copyright">
        &copy; Cassandra Tankó, {year}
    </p>
</footer>

<style lang="scss">
    @use "../../styles/base/vars" as *;
    @use "../../styles/base/mixins" as *;
    @use "../../styles/base/typography" as *;

    footer {
        max-width: 100%;
        padding: 7rem 1rem 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: baseline;
        gap: 1rem;

        .copyright {
            @include text-small;
            opacity: 0.555;
            margin-bottom: 0.5rem;
        }

        @include is-max-tablet {
            flex-direction: column;
            .copyright {
                margin-bottom: 0.333em;
                @include text-small;
            }
        }

        @include is-phone {
            gap: 0.777rem;
            padding: 3rem 0.75rem 0;
            .copyright {
                opacity: 0.555;
            }
        }
    }

    a {
        text-decoration: none;
        @media (hover: hover) and (pointer: fine) {
            &:hover {
                text-decoration: underline dotted;
            }
        }
    }

    .footlinx {
        display: flex;
        flex-wrap: wrap;
        gap: 1.444rem;

        .separator {
            font-size: 0.7em;
        }

        @include is-phone {
            width: 100%;
            flex-direction: column;
            gap: 0.25em;
            @include text-small;

            .separator {
                display: hidden;
            }
        }
    }
</style>
